# Habit status contract

A habit record may expose `active`, `paused`, or `archived` as its status. Only `active` habits are included in current completion metrics. `paused` habits remain visible in management views, while `archived` habits are excluded from default lists and exports unless explicitly requested.