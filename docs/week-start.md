# Week-start configuration

Weekly summaries use the configured week-start day consistently across cards, charts, exports, and date labels.

Changing the week-start preference changes grouping boundaries only; it must not change check-in dates or completion records. When no preference is stored, Monday is the default.
