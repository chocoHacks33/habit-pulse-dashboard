# Printable summary metadata

A printed summary should include the dashboard title, selected date range, generation timestamp, and the active filter description. Decorative controls and navigation should be hidden from print. The timestamp should be rendered in the user's locale while the underlying export keeps an ISO 8601 value for reproducibility.