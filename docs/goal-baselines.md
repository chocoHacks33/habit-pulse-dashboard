## Goal baselines

A goal baseline is the starting value used for progress calculations in a selected period. Baselines remain stable for the period and are replaced only when the user explicitly starts a new goal period. Empty or negative baselines should be rejected with an actionable validation message.