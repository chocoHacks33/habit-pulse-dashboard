# History Ordering

History entries should be ordered newest first so the latest activity is immediately visible.

When two entries share a date, preserve their recorded order. Missing dates should remain in the empty state rather than being sorted as a real calendar value.