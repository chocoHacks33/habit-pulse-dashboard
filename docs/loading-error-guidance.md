# Loading and Error States

Reserve space for summary cards while data loads and show a concise retry action on failure. Empty data is valid and should use the empty-habits message instead of an error message.