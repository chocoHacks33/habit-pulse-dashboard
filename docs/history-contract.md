# History data contract

History entries use `{ date, completed }`. `date` is an ISO calendar date and `completed` is boolean. Consumers should preserve missing dates rather than inferring completion.
