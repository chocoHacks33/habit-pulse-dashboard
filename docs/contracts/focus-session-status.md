# Focus-session status

A focus session is `planned`, `active`, `paused`, `completed`, or `cancelled`. Status transitions should be monotonic except that an active session may move to paused and resume as active.
