# Dashboard release checklist

- Verify the empty state with zero habits.
- Verify filters preserve the selected date range.
- Test completion controls with keyboard input.
- Check the dashboard at narrow and wide viewport widths.
- Confirm exported dates use ISO `YYYY-MM-DD` format.
