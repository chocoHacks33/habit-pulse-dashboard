# Local data retention

Habit records stay in the browser until the person removes them or clears site data. The dashboard does not require an account or send completion history to a server.

Exports are user-initiated snapshots. An export file can outlive the dashboard, so users should treat downloaded files as personal data and delete them when no longer needed.