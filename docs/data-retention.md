# Local data retention

Habit records remain in the browser’s local store until the user deletes them or clears application data. Exporting a file does not delete local records.

If a future sync feature is introduced, it must state its retention period and deletion behavior separately from this local-only policy.