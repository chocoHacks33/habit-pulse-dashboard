# Habit export schema

Exports use a JSON array of habit snapshots. Each item includes `name`, `done`, and `target`.

Consumers can calculate a bounded completion percentage with the shared completion helper.
