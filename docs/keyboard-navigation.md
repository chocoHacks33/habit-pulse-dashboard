# Keyboard navigation

Habit rows should be reachable in DOM order, expose their completion control as a button, and preserve a visible focus ring. Pressing `Enter` or `Space` on a focused completion control toggles the habit without moving focus.