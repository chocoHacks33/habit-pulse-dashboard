# Weekly History Data

Weekly summaries use `{ "date": "YYYY-MM-DD", "completed": number, "target": number }` entries so charts and exports share one stable contract.

Dates are calendar dates, not timestamps; clients should render them in the viewer's locale.