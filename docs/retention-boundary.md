# Local data retention boundary

Keep active habit records and their completion history until the user deletes them. A clear-data action must explain that it removes both current habits and historical check-ins, and should require an explicit confirmation before proceeding.