# Add dashboard release QA checklist

Release checks cover empty, populated, archived, narrow-screen, keyboard-only, reduced-motion, and export scenarios before a dashboard build is shipped.

## Review notes

- Confirm the behavior is represented in the UI or data contract.
- Keep empty and error states explicit.
- Add a focused test when this rule becomes executable.
