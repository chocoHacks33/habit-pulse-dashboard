# Dashboard release smoke checklist

Before release, verify the core path: create a habit, record a check-in, view the updated streak, change the date range, and export the visible results.

Also verify keyboard navigation, mobile layout, empty states, and that dates display consistently in the selected timezone.
