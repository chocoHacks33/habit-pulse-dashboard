## Export integrity

Exports include a stable header order, ISO dates, and explicit empty values. Before download, the export path should verify that every selected habit has an identifier and that filtered history uses the same timezone boundary as the dashboard.