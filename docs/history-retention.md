# History Retention

Habit history is local dashboard data. Features that remove or reset history should state the scope of the action before it occurs and should not imply server-side backup.

Exports should be user-initiated. Any future cleanup policy should preserve the current week by default and document the retention period before implementation.
