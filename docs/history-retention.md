# History retention

The dashboard keeps only the activity history needed for the configured reporting period. Users should be able to delete local history from the settings surface, and deletion should remove both daily entries and derived streak values.

Exported files are user-owned copies and are not changed when local history is deleted.