# Habit data model

A habit record has a non-empty name, a non-negative done count, a positive target count, and an optional category. Malformed numeric values should render as incomplete rather than throwing.
