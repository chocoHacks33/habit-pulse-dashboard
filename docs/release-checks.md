## Dashboard release checks

Before release, verify week-start configuration, timezone-safe day boundaries, archived-habit filtering, empty states, keyboard focus order, import previews, export headers, and offline retry messaging. Each check should be exercised with both populated and empty data.