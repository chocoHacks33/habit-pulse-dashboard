# Trend baseline calculation

Trend summaries compare the current complete period with the immediately preceding period of equal length. If the previous period has no observations, show an unavailable baseline instead of implying a zero-percent change.

## Acceptance criteria

- Comparisons use equal, non-overlapping date windows.
- Missing baselines are represented explicitly.
- Positive, negative, and unchanged trends have distinct text labels.
