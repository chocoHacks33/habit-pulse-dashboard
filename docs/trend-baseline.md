# Trend Baseline

Trend indicators compare the current completed-count window with the immediately preceding window of equal length.

If the preceding window has no observations, show “No baseline” instead of a positive or negative trend. Zero change should be presented as stable.