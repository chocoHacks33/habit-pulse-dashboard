# Empty-state actions

When no habits exist, the dashboard should offer one primary action: `Add your first habit`. A secondary `View example` action may load fixture data without changing the saved dataset. Empty weekly history should explain that completing a habit will create the first data point.