# Empty-state actions

When no habits exist, show a short explanation and two actions: `Create your first habit` as the primary action and `Import habits` as the secondary action. Keep the empty state visible until the list contains at least one active habit; archived habits alone should not suppress it.