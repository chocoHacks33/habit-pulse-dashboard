# Empty-state actions

When no habits exist, the dashboard should offer three clear actions:

- Create a habit with a suggested cadence.
- Import existing habits from a supported export.
- View a short example dashboard.

The empty state should explain that no data has been removed and should remain useful on mobile widths.