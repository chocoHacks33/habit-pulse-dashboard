# Empty-State Actions

When no habits exist, the dashboard should explain the next step in one sentence and offer a primary `Add a habit` action. A secondary example or starter option can reduce friction, but it must be clearly distinguishable from persisted user data.

The empty state should remain useful without JavaScript errors, and its action should receive focus when it is the only available task.
