# Empty-state actions

An empty dashboard should explain what is missing and offer one primary next action. Empty states differ by cause: no habits, no activity in the selected range, or filtered results with no matches.

## Acceptance criteria

- The heading identifies the empty condition.
- The primary action is keyboard reachable.
- Clearing a filter is offered when filters caused the empty result.
