# Check-in validation

The dashboard should reject check-ins that are missing a habit identifier or use a future timestamp. Keep validation messages actionable and preserve the original input for correction.
