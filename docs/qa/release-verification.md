# Release verification

Before release, verify the dashboard with zero habits, one habit, a full week, an archived habit, and an invalid imported date. Check keyboard navigation, reduced motion, narrow viewport layout, and CSV output.
