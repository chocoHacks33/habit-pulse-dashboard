# Dashboard Release Smoke Checklist

Before releasing a dashboard change, verify:

- The default view loads with sample data.
- Empty and error states remain readable.
- Keyboard focus is visible.
- Dates use the configured local day.
- Exported values retain stable headers.
- Reduced-motion preferences are respected.