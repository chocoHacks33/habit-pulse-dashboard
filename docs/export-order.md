# Export column order

Exported habit records use a stable column order so downstream spreadsheets remain predictable.

1. id
2. name
3. date
4. completed
5. note
6. createdAt

New optional columns should be appended rather than inserted between existing columns unless a versioned export format is introduced.
