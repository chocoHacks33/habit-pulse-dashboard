# Keyboard help guidance

The dashboard should expose a short keyboard help entry point. Suggested keys: `?` opens help, `Esc` closes dialogs, and `Tab` moves through controls in reading order. Never trap focus inside a non-modal panel.