# Keyboard help

The habit list should remain usable without a pointer:

- `Tab` moves through controls in reading order.
- `Enter` or `Space` activates the focused control.
- `Escape` closes transient help or export panels.

Focus indicators must remain visible against both dashboard themes.