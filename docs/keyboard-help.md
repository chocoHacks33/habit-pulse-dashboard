# Keyboard Help

The dashboard should expose a short keyboard help panel with:

- `Tab` and `Shift+Tab` to move between controls.
- `Enter` or `Space` to toggle a focused check-in.
- `Escape` to close dialogs and help panels.

The focused control must remain visibly outlined while the user navigates.
