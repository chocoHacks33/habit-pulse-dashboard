# Local retention boundary

Keep completed habit records and generated summaries in browser storage only when the user has opted into persistence. A clear-data action should remove records, cached exports, and preferences together, then confirm the empty state.