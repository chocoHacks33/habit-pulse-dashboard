# Weekly goal progress copy

Use `0 of 7 complete` when a week has no check-ins. When progress exists, show the completed count first and keep the goal denominator visible, for example `4 of 7 complete`.

If the goal is exceeded, preserve the achieved count and add a short success cue such as `Goal exceeded` rather than replacing the metric with a percentage.