# Export format

Habit exports use one row per habit and ISO dates for machine-readable fields. The recommended columns are `habit`, `date`, `completed`, and `streak`.

Consumers should treat unknown columns as optional so the dashboard can add metrics without breaking imports.