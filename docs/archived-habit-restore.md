# Archived Habit Restore

Archiving removes a habit from active daily work without deleting its history.

When restored, the habit returns to the active list with its existing identifier and historical check-ins. It should not retroactively create missed check-ins or alter prior streak calculations. The restore confirmation should state where the habit will reappear and whether reminders need to be re-enabled.