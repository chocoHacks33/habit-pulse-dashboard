# Keyboard focus order

The primary focus order is: range controls, summary metrics, habit filters, habit list actions, and export controls. Focus must remain visible and should not jump when a metric updates.
