# Import preview

Imports should be previewed before any local records are changed.

## Preview contents

- Number of valid records.
- Number of rejected records.
- The first actionable error for each rejected row.
- Whether imported identifiers would replace or create records.

The user must confirm a preview before the import is committed. A cancelled preview leaves existing data unchanged.
