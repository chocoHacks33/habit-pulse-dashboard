# Keyboard shortcuts

Keyboard shortcuts are optional accelerators for users who prefer them. Focus must remain visible, shortcuts must not override text-entry fields, and every shortcut action must also be available through visible controls.

## Suggested shortcuts

- `?` opens shortcut help.
- `g` focuses the goal summary.
- `h` focuses habit history.
- `e` opens export options.
