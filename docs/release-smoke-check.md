# Release Smoke Checks

1. Open `index.html` in a browser with JavaScript enabled.
2. Confirm the page title, heading, and habit list render without console errors.
3. Resize to a narrow viewport and confirm content remains readable.
4. Tab through controls and confirm visible focus.
5. Verify a zero-completion habit does not show `NaN` or an infinite percentage.
