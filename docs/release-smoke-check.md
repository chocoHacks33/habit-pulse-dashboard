# Release smoke checklist

Before release, verify:

- the dashboard loads with the sample data;
- completion changes update the visible totals;
- filters can be cleared;
- keyboard focus is visible;
- the empty state has a recovery action;
- print output hides interactive-only controls.