# Release smoke check

Before publishing a dashboard build, verify: the page loads without console errors, a habit can be added, a completion can be toggled, the weekly summary updates, and keyboard navigation reaches every primary action.