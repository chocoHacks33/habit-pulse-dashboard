# Week boundary rules

Weekly summaries use Monday as the first day and Sunday as the last day. The week key is the local date of that Monday.

A completion at `23:30` local time belongs to the local calendar day shown to the user, even if its ISO timestamp crosses a UTC date boundary. Partial weeks may be displayed, but should be labeled as partial.